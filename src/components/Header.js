import React from "react";

function Header(){
  const styledHeader= {
    textAlign: "center"
  }
  return(
    <div style={styledHeader}>
      <h1>Tap Room</h1>
    </div>
  )
}

export default Header;